@using DoctorsOffice.Models;
@{
  Layout = "_Layout";
}

<h1>List of Doctor's Specialties</h1>

<h2>Specialties</h2>
@if (@Model.Count == 0)
{
  <h3>No specialties have been added yet!</h3>
}
@foreach (Specialty specialty in Model)
{
  <li>@Html.ActionLink($"{specialty.SpecialtyName}", "Details", new { id = specialty.SpecialtyId}) </li>
}
<br>
<p>@Html.ActionLink("Add New Specialty", "Create", null, null, new {@class="btn btn-success btn-sm"})</p>